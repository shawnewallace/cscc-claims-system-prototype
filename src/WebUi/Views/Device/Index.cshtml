@model EDeviceClaims.WebUi.Models.DeviceListViewModel


<ul>
  @foreach (var thing in Model)
  {
    <li>
      @thing.Name -> @thing.SerialNumber
      @Html.Partial("_GetClaimButton", thing)
    </li>
  }
</ul>